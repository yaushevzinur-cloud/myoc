# MyOS Web V0.11.8 — Goal Task Inbox Fix

Исправлена совместимость задач цели с существующим планировщиком.

Что изменено:
- связанные задачи цели получают planner-поле `planLevel: "inbox"`;
- старые goal-задачи автоматически мигрируют во «Входящие» и получают `planLevel`;
- сохраняются `goalId`, `horizon`, статус, приоритет, длительность и сфера;
- карточка цели по-прежнему показывает числовой прогресс отдельно от задач;
- `config.js` не изменён.

Ожидаемый результат:
задачи 15/20/25, созданные из цели, должны появиться в «План → Входящие».
