# MyOS V0.12.5 — Language Save Recovery

Исправление точечно от рабочей V0.12.4:
- прямой переход «Я → Языки» сохранён;
- исправлен цикл после «Сохранить занятие»: после save() больше нет второго принудительного languages();
- ensureLanguages() восстанавливает/нормализует уже записанные language-данные из localStorage/Supabase;
- история и заметки приводятся к безопасному формату;
- Goals, Planner и config.js не изменялись.
