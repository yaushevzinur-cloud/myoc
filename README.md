# MyOS Web V0.11.1 — Goals Navigation Fix

Исправлено:
- «🎯 Цели и приоритеты» в разделе «Я» теперь открывает отдельный экран;
- можно создавать цель, задавать сферу, приоритет, срок;
- прогресс ±10%, пауза, завершение и возврат цели;
- из цели создаётся связанная задача во «Входящие»;
- задача получает `goalId` и остаётся частью обычного планировщика.

Остальные направления в разделе «Я» пока остаются будущими модулями.
Cloud Sync и `config.js` не менялись.
